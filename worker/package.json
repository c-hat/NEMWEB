{
  "name": "nemweb-live-pinger",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker cron that triggers the NEMWEB live-data GitHub workflow every ~10 min.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5.7.3",
    "wrangler": "^4"
  }
}
