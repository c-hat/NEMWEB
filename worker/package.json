{
  "name": "nemweb-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Edge caching proxy for OpenElectricity live data, for the NEMWEB tracker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5.7.3",
    "wrangler": "^4"
  }
}
